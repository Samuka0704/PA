programa {
  funcao inicio() {

    real base,altura,area

    escreva("digite base")
     leia(base)
    escreva("digite altura")
    leia(altura)

    area = (base*altura)

    escreva("a area e"´, area)
    
  }
}
